#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name="lti-resize-frame",
    version="1.0.0",
    description="Plugin to post an iframe size for LTI courses",
    packages=find_packages(),
    install_requires=[],
    tests_require=[],
    extras_require={},
    scripts=[],
    include_package_data=True,
    author="François Michel",
    license="AGPL 3",
)
